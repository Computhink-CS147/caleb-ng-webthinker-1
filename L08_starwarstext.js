// write your codes here
function setup(){
    
}
function draw(){
    // background(220);
    // textSize(20);
    textSize(20);
    textAlign(LEFT);
    text("My Favourite Foods");
    for(let i=0; i<FileList,length;i++);{
        text(list[1],50*50)
    }
}